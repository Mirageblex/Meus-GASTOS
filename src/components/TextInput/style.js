import { StyleSheet } from "react-native";
const TextoInputEstilo = StyleSheet.create({
    container: {
      marginVertical: 10,
    },
    input: {
      borderWidth: 1,
      borderColor: '#ccc',
      borderRadius: 5,
      padding: 10,
    },
  });
  
  export default TextoInputEstilo;